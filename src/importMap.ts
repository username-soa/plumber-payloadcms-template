/**
 * This file is used by PayloadCMS to map imports for the admin panel.
 * It will be automatically updated when collections are added.
 */

import { SlugField as SlugField_0 } from "@/components/payload/SlugField";

export const importMap = {
	"@/components/payload/SlugField": SlugField_0,
};
